# AI 游戏设计大赛 2026 · 落地页

Vite + React + TypeScript + Tailwind CSS v4。

## 本地运行

```bash
npm install
npm run dev
```

首屏背景视频：将 `hero-bg-ai-contest_2.mp4` 放在 `public/` 目录。文件较大时，可在 `.gitignore` 中加入 `public/*.mp4`，仅本地保留该文件以减小仓库体积。

## 构建

```bash
npm run build
npm run preview
```
